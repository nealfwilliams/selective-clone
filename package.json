{
  "name": "selective-clone",
  "version": "0.1.0",
  "description": "Makes copies that are safely mutable",
  "engines": {
		"node": ">=8"
	},
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir dist"
  },
  "author": "Neal Williams",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "jest": "^21.2.1"
  }
}
